#include<bits/stdc++.h>
using namespace std;

#define ll long long
#define INF 99999999
#define rep(i, n) for(int i = 0; i < (int)(n); i++)
#define all(x) (x).begin(),(x).end()




int main()
{
    ll x,y,z,k;
    cin >> x >> y >> z >> k;

    vector<ll> a(x);
    rep(i,x) cin >> a[i];

    vector<ll> b(y);
    rep(i,y) cin >> b[i];

    vector<ll> c(z);
    rep(i,z) cin >> c[i];

}
